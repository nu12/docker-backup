<h1>Recover#new</h1>
<p>Find me in app/views/recover/new.html.erb</p>

<%= form_with(url: "/recover/create", method: "post") do |form| %>
  
    <div class="form-group">
        <%= form.label :name %>
        <%= form.text_field :name, value: @name, readonly: "readonly", class: "form-control"  %>
    </div>

    <div class="form-group">
        <%= form.label :volume %>
        <%= form.text_field :volume, value: @name, class: "form-control" %>
    </div>

    <div class="actions">
        <%= form.submit "Create", class: "btn btn-primary form-control" %>
    </div>
<% end %>